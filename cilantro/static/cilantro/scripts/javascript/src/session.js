// Generated by CoffeeScript 1.3.3
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define(['environ', 'mediator', 'underscore', 'backbone'], function(environ, mediator, _, Backbone) {
  var Preferences;
  Preferences = (function(_super) {

    __extends(Preferences, _super);

    function Preferences() {
      return Preferences.__super__.constructor.apply(this, arguments);
    }

    Preferences.prototype.url = environ.absolutePath('/api/preferences/');

    Preferences.prototype.defaults = {
      session: {}
    };

    Preferences.prototype.initialize = function() {
      var _this = this;
      mediator.subscribe('session/save', function(key, data) {
        var session;
        session = _this.get('session');
        session[key] = data;
        return _this.save({
          session: session
        });
      });
    };

    Preferences.prototype.load = function() {
      var data, key, _ref;
      _ref = this.attributes.session;
      for (key in _ref) {
        data = _ref[key];
        mediator.publish('session/load/' + key, data);
      }
    };

    return Preferences;

  })(Backbone.Model);
  return App.preferences = new Preferences(App.preferences);
});
